
# 🛰️ Ping‑Trace‑API

سرویسی سبک و سریع مبتنی بر **FastAPI** برای اجرای دستورات شبکه‌ای **Ping** و **Traceroute** روی سرور.  
طراحی شده برای استفاده در ابزارهای کلاینتی مانند **PyQt Network Utility** یا هر رابط REST دیگر.

---

## 🚀 نحوه استفاده
پس از اجرای سرور (به‌صورت لوکال یا Docker):

### ✅ Ping
curl http://localhost:8000/ping/8.8.8.8
```
خروجی:
```json
{"reachable": true}
```

### 🌐 Traceroute
curl http://localhost:8000/trace/8.8.8.8
```
خروجی:
```json
{"trace": "hop1 → hop2 → hop3 ..."}
```

---

## ⚙️ پیش‌نیازها
- Python ≥ 3.11  
- Docker ≥ 24 *(اختیاری برای اجرای کانتینری)*  
- ابزارهای شبکه: `ping`, `traceroute`

---

## ✨ ویژگی‌ها
- اجرای سریع با **FastAPI**  
- پشتیبانی از دستورهای `ping` و `traceroute`  
- مناسب برای استقرار در **VPS / CI/CD Pipelines**  
- طراحی REST‑Friendly و قابل استفاده در فرانت PyQt  
- قابلیت توسعه آسان برای ابزارهای شبکه سفارشی  

---
