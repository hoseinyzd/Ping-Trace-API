#version: '3.9'

services:
  pingtrace:
    build: .
    container_name: pingtrace-api
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"     
    networks:
      - Main    


networks:
  Main:
    name: Main
    driver: bridge
